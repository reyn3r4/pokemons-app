import { ImgSliderDirective } from './img-slider.directive';

describe('ImgSliderDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgSliderDirective();
    expect(directive).toBeTruthy();
  });
});
