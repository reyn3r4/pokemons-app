import { GenPokemonsResumePipe } from './gen-pokemons-resume.pipe';

describe('GenPokemonsResumePipe', () => {
  it('create an instance', () => {
    const pipe = new GenPokemonsResumePipe();
    expect(pipe).toBeTruthy();
  });
});
